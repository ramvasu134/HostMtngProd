<!--
  Copy these <uses-permission> + <uses-feature> elements into the
  <manifest> root of android/app/src/main/AndroidManifest.xml
  (BEFORE the <application> block).

  These are the bare-minimum permissions required by Host Mtng to run
  WebRTC video meetings on Android.
-->

<uses-permission android:name="android.permission.INTERNET" />
<uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
<uses-permission android:name="android.permission.RECORD_AUDIO" />
<uses-permission android:name="android.permission.CAMERA" />
<uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
<uses-permission android:name="android.permission.WAKE_LOCK" />
<uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
                 android:maxSdkVersion="32" />

<!-- Hardware features are declared as not required so the app still installs
     on devices without one of them (e.g. tablets without a rear camera). -->
<uses-feature android:name="android.hardware.camera"     android:required="false" />
<uses-feature android:name="android.hardware.microphone" android:required="false" />
